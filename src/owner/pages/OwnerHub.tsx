// OwnerHub — superseded by the unified OwnerHome (`/owner`). Kept as a thin
// re-export so existing /owner/hub links continue to resolve to the new desk.

export { OwnerHome as OwnerHub } from "@/owner/pages/OwnerHome";
