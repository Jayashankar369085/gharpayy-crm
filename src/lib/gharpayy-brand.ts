export type Pitch = {
  why: string;
  how: string;
  next: string;
};

export const GHARPAYY_BRAND = {
  name: "Gharpayy",
  promise: "Direct-to-owner Bengaluru rentals with verified rooms, fast tours and weekly referral payouts.",
  primaryCta: "Find the right home",
};
